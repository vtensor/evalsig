"""Optional helpers for fitting evalsig into existing tooling.

Each submodule has zero hard dependencies on its target system; the
imports happen lazily so missing packages do not block evalsig install.
"""
